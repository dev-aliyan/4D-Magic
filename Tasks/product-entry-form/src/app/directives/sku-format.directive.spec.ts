import { SkuFormatDirective } from './sku-format.directive';

describe('SkuFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new SkuFormatDirective();
    expect(directive).toBeTruthy();
  });
});
