export class Todo {
    title : string
    description : string
    active : boolean

    constructor (title: string, description: string, active: boolean ) {
        this.title = title
        this.description = description
        this.active = active
    }
}

